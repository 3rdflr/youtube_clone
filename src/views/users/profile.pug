extends ../layout
include ../mixins/video

block content
    div.profile__data
        img(src=user.avatarUrl).avatar
        h3=user.name
        br
        br
        div.bound_box
            span #{user.bound} bound
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found